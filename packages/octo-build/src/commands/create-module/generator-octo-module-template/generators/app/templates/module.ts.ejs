import { AModule, Module, type <%= moduleTypePascal %> } from '@quadnix/octo';
import { <%= moduleNamePascal %>ModuleSchema } from './index.schema.js';

@Module<<%= moduleNamePascal %>Module>('<%= packageName %>', <%= moduleNamePascal %>ModuleSchema)
export class <%= moduleNamePascal %>Module extends AModule<<%= moduleNamePascal %>ModuleSchema, <%= moduleTypePascal %>> {
  async onInit(inputs: <%= moduleNamePascal %>ModuleSchema): Promise<<%= moduleTypePascal %>> {
    // TODO: Create and return a new instance of <%= moduleTypePascal %>.
  }
}
