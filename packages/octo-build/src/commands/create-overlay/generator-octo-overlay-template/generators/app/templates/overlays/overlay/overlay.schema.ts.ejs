import { BaseOverlaySchema, Schema, Validate } from '@quadnix/octo';

export class <%= moduleNamePascal %><%= overlayNamePascal %>OverlaySchema extends BaseOverlaySchema {
  @Validate({
    destruct: (value: <%= moduleNamePascal %><%= overlayNamePascal %>OverlaySchema['properties']): string[] => [
      // Fix me: Add properties for validation.
    ],
    options: { minLength: 1 },
  })
  override properties = Schema<{
    // Fix me: Add overlay properties.
  }>();
}
