import { AResource, Resource } from '@quadnix/octo';
import { HttpFetchSchema } from './index.schema.js';

/**
 * This is an example resource that demonstrates how to create a custom resource.
 */
@Resource<HttpFetch>('@<%= name %>', 'http-fetch', HttpFetchSchema)
export class HttpFetch extends AResource<HttpFetchSchema, HttpFetch> {
  constructor(resourceId: string, properties: HttpFetchSchema['properties'], parents: []) {
    super(resourceId, properties, parents);
  }
}
